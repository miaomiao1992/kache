[package]
name = "rust-check"
version = "0.1.0"
edition = "2021"

# Empty `[workspace]` keeps cargo from walking upward into the kache
# repo's own workspace — see multi-dep.
[workspace]

[dependencies]
itoa = "1"
